<!DOCTYPE html>
<html>
<head>
   <title>Bingo</title>
   <meta charset="UTF-8">
</head>

<style>
body {
    margin: 0;
    padding: 0;
    height: 100vh;
    background-color: #e63946;
    justify-content: center;
    align-items: center;
    display: flex; 
    font-family: arial;
}

.carton {
     flex-direction: column;
     display: flex;    
     border: 4px solid #1d3557;
}

.fila {
     display: flex;
}


.fila > div {
      border: 2px solid #1d3557;
      width:  6em;
      height: 6em;
      justify-content: center;
      text-align: center;
      align-items: center;
      display: flex;
}


.celda {
     background-color: #f1faee;
     color: #1d3557;
}

.celda_vacia {
     background-color: #a8dadc;
}


h1 {
   font-size: 4em;         
}

</style>


<body>
<div class = "carton">
    {% for fila in carton %} 
    <div class = "fila">
        {% for celda in fila %}
           {% if celda == 0 %}
              <div class = "celda_vacia"></div>
           {% else %}
              <div class = "celda"> <h1> {{ celda }} </h1> </div>
           {% endif %} 
        {% endfor %}
    </div>
    {% endfor %}
</div>              
</body>

</html>


